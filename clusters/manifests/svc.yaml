apiVersion: v1
kind: Service
metadata:
  name: demo-svc
  labels:
    app: distilgpt2
spec:
  type: LoadBalancer
  selector:
    app: distilgpt2
  ports:
    - port: 80
      targetPort: 3003
      protocol: TCP
      name: http
